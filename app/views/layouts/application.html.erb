<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Xpense Magick" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= javascript_include_tag "jquery", "application" %>
    <%= yield(:page_javascript) if content_for?(:page_javascript) %>

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= stylesheet_link_tag "flatly", :media => "all" %>
    <%= yield(:page_stylesheet) if content_for?(:page_stylesheet) %>    
    <!-- Le styles -->

  </head>
  <body>
    <div class="container-fluid" id="mg-page-wrap">
      <div class="row-fluid" id="mg-app-container">
        <div class="navbar <%= "navbar-default" %> navbar-fixed-top">
            <div class="navbar-header col-sm-3">
              <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-main">
                <i class="icon-list" style="color: #FFF;"></i>
              </button>
              <a class="navbar-brand" href="#">Xpense Magick</a>
            </div>
            <% unless current_user %>
              <nav class="navbar-collapse collapse in" id="navbar-main" role="navigation">
                <%= render(:partial => "common/non_logged_in_tabs") %>
              </nav><!--/.nav-collapse -->
            <% end %>
        </div>
      
        <%# Flash Container %>
        <%= render :partial => "common/flashdisplay", :locals => {:flash => flash} %>
        
        <%# Main Container %>
        <%= render :partial => "common/#{"non_" unless current_user}logged_in_page" %>
      </div>
    </div>
  </body>
</html>

<script type="text/javascript">
  jQuery(document).ready(function() {
    jQuery('.dropdown-toggle').dropdown();
    jQuery('.collapse').collapse({ toggle: true});
  });
</script>
