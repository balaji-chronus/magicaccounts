<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Magic Accounts" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= javascript_include_tag "jquery", "application" %>
    <%= javascript_include_tag "flatly" %>
    <%= yield(:page_javascript) if content_for?(:page_javascript) %>

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= stylesheet_link_tag "flatly", :media => "all" %>
    <%= yield(:page_stylesheet) if content_for?(:page_stylesheet) %>    
    <!-- Le styles -->

  </head>
  <body>
    <div class="container-fluid">
      <div class="row-fluid">
        <div class="navbar <%= current_user ? "navbar-default" : "navbar-inverse" %> navbar-fixed-top">
            <div class="navbar-header">
              <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>
              <a class="navbar-brand" href="#">Magic Accounts</a>
            </div>
            <div class="navbar-collapse collapse">
              <% if current_user %>
                <ul class="nav navbar-nav">
                  <li>
                    <a id="mg-add-transaction" href="#">
                      <button type="button" class="btn btn-success btn-xs"><i class="icon-plus-sign"></i> &nbsp; Add Transaction</button>
                    </a>
                  </li>
                </ul>
                <ul class="nav navbar-nav navbar-right pull-right">
                 <li>
                  <%= gravatar_image_tag('seetharaman.balaji@gmail.com', :gravatar => {:size => 48}, :class => "pull-right", :style => "-webkit-border-radius: 100px; border-radius: 100px; margin: 6px 20px;") %>
                  </li>
                </ul>
                <% else %>
                  <ul class="nav navbar-nav">
                    <%= render :partial => "common/non_logged_in_tabs" %>
                  </ul>
                <% end %>
            </div><!--/.nav-collapse -->
        </div>
        <%= render :partial => "common/flashdisplay", :locals => {:flash => flash} %>
    
        <%= render :partial => "common/#{"non_" unless current_user}logged_in_page" %>
      </div>
    </div>
  </body>
</html>

<script type="text/javascript">
  jQuery(document).ready(function() {
    jQuery('.dropdown-toggle').dropdown();
    jQuery(".collapse").collapse();
  });
</script>
