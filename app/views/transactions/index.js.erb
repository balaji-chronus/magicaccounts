jQuery('#mg-transaction-entries').append('<%= j render(:partial => "transactions/transactions_entries", :locals => {:entries => @transactions}) %>');
<% if @transactions.next_page %>
  jQuery('.pagination').replaceWith('<%= j will_paginate(@transactions) %>');
<% else %>
  jQuery('.pagination').remove();
<% end %>